import {LiaStarSolid as Star} from 'react-icons/lia'

const StarRating = ({starsAmmount = 5}) => {

  return (
    [...Array(starsAmmount)].map((star, index) => {
      
    })
  )
}

export default StarRating